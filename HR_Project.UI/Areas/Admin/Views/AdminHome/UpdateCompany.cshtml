@using HR_Project.UI.Areas.Admin.Models
@model UpdateCompanyVM

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layouttt.cshtml";
}
<style>
    .table img {
        max-width: 50px; /* Maksimum genişlik */
        max-height: 50px; /* Maksimum yükseklik */
        object-fit: cover; /* Orantılı olarak fotoğrafı sığdır */
        border-radius: 50%; /* Yuvarlak kenarlıklar */
    }

    span {
        color: white;
    }

    th {
        font-size: 16px;
        font-weight: bold;
    }

    td {
        font-size: 13px;
    }

    .pclass {
        margin-top: 30px;
    }

    label {
        color: white;
    }

    .selectDesign {
        padding: 10px 15px;
        background-color: #FFFFFF14;
    }

    .normalTd {
        margin-top: -5px;
    }

    .optionColor > option {
        background-color: #FFFFFF14;
        color: black;
    }

    .formDesign {
        padding: 30px;
        background: rgba(255, 255, 255, 0.08);
        margin: 0;
        font-family: var(--bs-body-font-family);
        font-size: var(--bs-body-font-size);
        font-weight: var(--bs-body-font-weight);
        line-height: var(--bs-body-line-height);
        color: var(--bs-body-color);
        text-align: var(--bs-body-text-align);
        background-color: rgba(255, 255, 255, 0.08);
        -webkit-text-size-adjust: 100%;
        -webkit-tap-highlight-color: transparent;
    }

    .form-group select {
        background-color: rgba(255, 255, 255, 0.08);
        border: hidden;
        border-block-color: rgba(255, 255, 255, 0.08);
        color: white;
    }

        .form-group select:focus {
            background-color: rgba(255, 255, 255, 0.08) !important;
            color: white;
        }

    .form-group input {
        background-color: rgba(255, 255, 255, 0.08);
        border: hidden;
        border-block-color: rgba(255, 255, 255, 0.08);
        color: white;
    }

        .form-group input:focus {
            background-color: rgba(255, 255, 255, 0.08) !important;
            color: white;
        }

    .pagination {
        display: flex;
        justify-content: center;
        list-style-type: none;
        margin: 0;
        padding: 0;
    }

        .pagination li {
            margin: 0 5px;
        }

            .pagination li a {
                color: black;
                text-decoration: none;
                padding: 5px 10px;
                border: 1px solid #ccc;
                border-radius: 3px;
            }

                .pagination li a.active {
                    background-color: #ccc;
                }

                .pagination li a:hover {
                    background-color: #f5f5f5;
                }
</style>
<head>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" rel="stylesheet" />

</head>
<section id="contact" class="contact section-show">
    <div class="container">
        <div class="section-title">
            <h2>Update</h2>
        </div>
        <form asp-action="UpdateCompany" asp-controller="AdminHome" asp-area="Admin" method="post" enctype="multipart/form-data">
            <div class="row g-0">
                <div class="col-lg-6">
                    <div class="p-5">
                        <div class="row">
                            <input type="hidden" asp-for="OriginalCompany.ID" class="form-control form-control-md" />
                            <div class="col-md-6 mb-4 pb-2">
                                <div class="form-outline">
                                    <label class="form-label" asp-for="OriginalCompany.CompanyName">Company Name</label>
                                    <input asp-for="OriginalCompany.CompanyName" class="form-control form-control-md" />
                                    <span asp-validation-for="OriginalCompany.CompanyName" class="text-danger"></span>
                                </div>
                            </div>

                            <div class="col-md-6 mb-4 pb-2">
                                <div class="form-outline">
                                    <label class="form-label" asp-for="OriginalCompany.CompanyTitle">Company Title</label>
                                    <select asp-for="OriginalCompany.CompanyTitle" class="form-control">
                                        <option value="">Select a Title</option>
                                        @foreach (var item in Model.TitleList)
                                        {
                                            <option value="@item.Value">@item.Text</option>
                                        }
                                    </select>

                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-4 pb-2">
                                <div class="form-outline">
                                    <label class="form-label" asp-for="OriginalCompany.MersisNo">Mersis Number</label>
                                    <input asp-for="OriginalCompany.MersisNo" class="form-control form-control-md" />
                                    <span asp-validation-for="OriginalCompany.MersisNo" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6 mb-4 pb-2">
                                <div class="form-outline">
                                    <label class="form-label" asp-for="OriginalCompany.TaxNumber">Tax Number</label>
                                    <input asp-for="OriginalCompany.TaxNumber" class="form-control form-control-md" />
                                    <span asp-validation-for="OriginalCompany.TaxNumber" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label class="form-label" asp-for="OriginalCompany.TaxOffice">Tax Office</label>
                                <input asp-for="OriginalCompany.TaxOffice" class="form-control form-control-md" />
                                <span asp-validation-for="OriginalCompany.TaxOffice" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label asp-for="OriginalCompany.Logo" for="image-upload" class="form-label">Please Choose An Logo</label>
                                <input asp-for="@Model.LogoPath" name="files" type="file" id="image-upload" accept=".png, .jpg, .jpeg" class="form-control form-control-md" value="@Model.LogoPath" />
                                <span asp-validation-for="OriginalCompany.Logo" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label class="form-label" asp-for="OriginalCompany.PhoneNumber">Phone Number</label>
                                <input asp-for="OriginalCompany.PhoneNumber" class="form-control form-control-md" />
                                <span asp-validation-for="OriginalCompany.PhoneNumber" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label class="form-label" asp-for="OriginalCompany.Address">Address</label>
                                <input asp-for="OriginalCompany.Address" class="form-control form-control-md" />
                                <span asp-validation-for="OriginalCompany.Address" class="text-danger"></span>
                            </div>
                        </div>

                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="p-5">
                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label class="form-label" asp-for="OriginalCompany.Email">Email</label>
                                <input asp-for="OriginalCompany.Email" class="form-control form-control-md" />
                                <span asp-validation-for="OriginalCompany.Email" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="mb-4 pb-2">
                            <div class="form-outline">
                                <label class="form-label" asp-for="OriginalCompany.NumberOfEmployees">Number Of Employees</label>
                                <input asp-for="OriginalCompany.NumberOfEmployees" class="form-control form-control-md" />
                                <span asp-validation-for="OriginalCompany.NumberOfEmployees" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline form-white">
                                <label asp-for="OriginalCompany.YearOfFoundation" class="control-label">Year Of Foundation</label>
                                <input style="width:568.8px; height:38.18px;" asp-for="OriginalCompany.YearOfFoundation" class="dateInput form-control form-control-md" type="date" data-date="" data-date-format="DD MMMM YYYY">
                                <span asp-validation-for="OriginalCompany.YearOfFoundation" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline form-white">
                                <label asp-for="OriginalCompany.ContractStartDate" class="control-label">Contract Start Date</label>
                                <input style="width:568.8px; height:38.18px;" asp-for="OriginalCompany.ContractStartDate" class="dateInput form-control form-control-md" type="date" data-date="" data-date-format="DD MMMM YYYY">
                                <span asp-validation-for="OriginalCompany.ContractStartDate" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-4 pb-2">
                            <div class="form-outline form-white">
                                <label asp-for="OriginalCompany.ContractFinishDate" class="control-label">Contract Finish Date</label>
                                <input style="width:568.8px; height:38.18px;" asp-for="OriginalCompany.ContractFinishDate" class="dateInput form-control form-control-md" type="date" data-date="" data-date-format="DD MMMM YYYY">
                                <span asp-validation-for="OriginalCompany.ContractFinishDate" class="text-danger"></span>
                            </div>
                        </div>

                        <input type="submit" value="Update Company" class="btn btn-primary" />
                    </div>
                </div>
            </div>
        </form>
    </div>
</section><!-- End Contact Section -->                  <!--CONTACT EKRANI            -->
<script>
    function submitForm() {
        var form = document.getElementById('updateUserForm');

        var fileInput = document.getElementById('image-upload');
        var file = fileInput.files[0];
        var fileSize = file.size;
        var maxSizeInBytes = 4 * 1024 * 1024; // Maksimum 4MB (4 * 1024 * 1024 byte)
        if (fileSize > maxSizeInBytes) {
            alert("Dosya boyutu çok büyük! Lütfen maksimum 4MB boyutunda bir dosya seçin.");
            return;
        }

        var fileExtension = file.name.split('.').pop().toLowerCase();
        if (fileExtension !== 'png' && fileExtension !== 'jpg' && fileExtension !== 'jpeg') {
            alert("Geçersiz dosya uzantısı! Sadece .png, .jpg veya .jpeg uzantılı dosyaları seçin.");
            return;
        }

        // FormData nesnesini oluşturarak form verilerini alın
        var formData = new FormData(form);
        // Dosyayı formData'ya ekleyin
        formData.append('files', file);

        // FormData'yı doğru bir şekilde göndermek için XMLHttpRequest kullanın
        var xhr = new XMLHttpRequest();
        xhr.open('POST', form.action);
        xhr.onload = function () {
            if (xhr.status === 200) {
                alert("Güncelleme işlemi başarıyla tamamlandı!");
                window.location.href = '/CompanyManager/CompanyManagerHome/Index'; // Index sayfasına yönlendirme
            } else {
                alert("Güncelleme işlemi sırasında bir hata oluştu.");
            }
        };
        // Content-Type başlığını ayarlamaya gerek yok, otomatik olarak ayarlanır.
        xhr.send(formData);
    }
</script>

<script>
    function submitForm() {
        var form = document.getElementById('updateUserForm');

        var fileInput = document.getElementById('image-upload');
        var file = fileInput.files[0];
        var fileSize = file.size;
        var maxSizeInBytes = 4 * 1024 * 1024; // Maksimum 4MB (4 * 1024 * 1024 byte)
        if (fileSize > maxSizeInBytes) {
            alert("Dosya boyutu çok büyük! Lütfen maksimum 4MB boyutunda bir dosya seçin.");
            return;
        }

        var fileExtension = file.name.split('.').pop().toLowerCase();
        if (fileExtension !== 'png' && fileExtension !== 'jpg' && fileExtension !== 'jpeg') {
            alert("Geçersiz dosya uzantısı! Sadece .png, .jpg veya .jpeg uzantılı dosyaları seçin.");
            return;
        }

        // FormData nesnesini oluşturarak form verilerini alın
        var formData = new FormData(form);
        // Dosyayı formData'ya ekleyin
        formData.append('files', file);

        // FormData'yı doğru bir şekilde göndermek için XMLHttpRequest kullanın
        var xhr = new XMLHttpRequest();
        xhr.open('POST', form.action);
        xhr.onload = function () {
            if (xhr.status === 200) {
                alert("Güncelleme işlemi başarıyla tamamlandı!");
                window.location.href = '/CompanyManager/CompanyManagerHome/AddUser'; // Index sayfasına yönlendirme
            } else {
                alert("Güncelleme işlemi sırasında bir hata oluştu.");
            }
        };
        // Content-Type başlığını ayarlamaya gerek yok, otomatik olarak ayarlanır.
        xhr.send(formData);
    }
</script>


